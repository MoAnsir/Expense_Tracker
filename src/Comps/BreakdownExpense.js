import { Row, Col, Button } from "react-bootstrap";
import { useState } from "react";

function BreakdownExpense({}) {
    return (
        <Row>
            <Col>
                <p>Breakdown Expense</p>
            </Col>
        </Row>
    );
}

export default BreakdownExpense;
